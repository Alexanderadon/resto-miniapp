// Public API слайса entities/cart (контракт №2).

export { useCartStore } from "./model/cart-store";
export { totalCount, totalTenge } from "./model/selectors";
export { MAX_ITEM_QUANTITY, MIN_ITEM_QUANTITY } from "./model/types";
export type { CartItem } from "./model/types";
